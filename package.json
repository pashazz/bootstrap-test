{
  "name": "bootstrap-test",
  "version": "1.0.0",
  "description": "something",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server"
  },
  "author": "pashazz",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  }
}
